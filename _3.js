function checkAge(age) {
    if (age > 18) {
      return true;
    } else {
      return confirm('Родители разрешили?');
    }
  }


  function checkAge2(age) { (age > 18) ? return true : return confirm('Родители разрешили?');}

  

  function checkAge3(age) { (age > 18) ? return true : return confirm('Родители разрешили?');}